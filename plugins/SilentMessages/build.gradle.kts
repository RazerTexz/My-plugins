version = "1.0.1"
description = "Type '@silent' at the start of your message"

aliucord {
    changelog = """
        # 1.0.1
        * Added a indicator for silent messages (the icon is temporary)

        # 1.0.0
        * Initial version
    """.trimIndent()

    deploy = true
}