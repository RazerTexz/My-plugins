version = "1.0.0"
description = "Automatically marks attachments as spoilers"

aliucord {
    changelog.set("""
        # 1.0.0
        * Initial version
    """.trimIndent())

    excludeFromUpdaterJson.set(false)
}
