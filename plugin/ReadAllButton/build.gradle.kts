version = "1.0.0"
description = "WARNING: I cannot guarantee you won't get banned!"

aliucord {
    changelog.set("""
        # 1.0.0
        * Initial version
    """.trimIndent())

    excludeFromUpdaterJson.set(false)
}
