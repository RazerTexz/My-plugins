version = "1.0.2"
description = "Prevents username truncation"

aliucord {
    changelog.set("""
        # 1.0.0
        * Initial version
    """.trimIndent())

    excludeFromUpdaterJson.set(false)
}
