version = "1.0.0"
description = "Adds a copy button on multi-line codeblocks!"

aliucord {
    changelog.set("""
        # 1.0.0
        * Initial version
    """.trimIndent())

    excludeFromUpdaterJson.set(false)
}