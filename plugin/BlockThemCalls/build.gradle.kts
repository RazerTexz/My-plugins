version = "1.0.0"
description = "Allows you to block calls from other users"

aliucord {
    changelog.set("""
        # 1.0.0
        * Initial version
    """.trimIndent())

    excludeFromUpdaterJson.set(false)
}
